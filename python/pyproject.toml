[project]
name = "iherb-holo-demo"
version = "0.1.0"
description = "Discover a durable iHerb product catalog with H's hosted browser agent"
requires-python = ">=3.11"
dependencies = [
    "hai-agents[cli]>=1.0.0",
    "httpx>=0.28.1",
    "pillow>=12.3.0",
    "pydantic>=2.0",
]
